# Development Dockerfile - Hot Reload Enabled
#
# USAGE:
#   docker compose up
#   or: docker build -f Dockerfile.dev -t sekuire/google-workspace-agent:dev .
#
# Hot-reload via tsx watch - code changes are reflected immediately.

FROM node:22-alpine
WORKDIR /app

RUN corepack enable && corepack prepare pnpm@latest --activate

COPY package.json pnpm-lock.yaml* ./
RUN pnpm install

COPY . .

ENV PORT=8002
EXPOSE 8002

CMD ["pnpm", "dev"]
